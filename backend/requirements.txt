# Layer 1 — Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# Security / Scanning
pyclamd==0.4.0

# ── Layer 2 — Document Processing ──────────────────────────────────────────
# docTR: OCR for scanned PDFs and images
python-doctr[torch]==0.7.0

# pdf2image: used by docTR for PDF-to-image conversion (requires poppler system package)
pdf2image==1.17.0

# pdfplumber: fast text extraction for native (non-scanned) PDFs
# Much faster than docTR for clean PDFs — used as the first-pass extractor
pdfplumber==0.11.0

# python-docx: DOCX / Word document text extraction
python-docx==1.1.2

# Pillow: image handling (required by docTR and pdfplumber)
Pillow==10.3.0

# ── Utilities ───────────────────────────────────────────────────────────────
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.25.2
ollama==0.1.7